# Environment Variables Example
# Copy this file to .env and modify as needed

# Application Settings
# Port for the application to run on (default: 3131)
PORT=3131

# Host address for binding (default: 0.0.0.0)
# Use 0.0.0.0 to allow external connections
# Use 127.0.0.1 or localhost for local-only access
HOST=0.0.0.0

# Docker Settings
# These variables are used by docker-compose.yml
# You can override them when running docker compose commands:
# PORT=3131 docker compose up -d

# Examples:
# PORT=3000    # Run on port 3000
# PORT=3131    # Run on port 3131
# PORT=5173    # Run on port 5173 (Vite default dev port)
